# Native helper tests

Run the native test loop with:

```sh
tests/native/run.sh
```

The harness is intentionally dependency-free. Tests compile the production helper source against only the Pebble types required by its tested contracts.
